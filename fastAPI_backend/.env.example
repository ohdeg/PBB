# Sranko ML FastAPI
# Copy DigitalCloset use.pth → models/use.pth (gitignored)
# Then: uv sync   OR   pip install -e .
#        uvicorn main:app --reload --host 127.0.0.1 --port 8000

SRANKO_MODEL_PATH=
SRANKO_WORN_GARMENT_EXTRACTION_ENABLED=true
