# E2E profile overlays (combine with: SPRING_PROFILES_ACTIVE=dev,e2e)
app:
  e2e:
    user-email: ${E2E_USER_EMAIL:e2e@pbb.test}
    user-password: ${E2E_USER_PASSWORD:E2ePassw0rd!}
    user-nickname: ${E2E_USER_NICKNAME:e2euser}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173,http://localhost:4173,http://127.0.0.1:4173}
